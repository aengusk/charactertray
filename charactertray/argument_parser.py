import argparse

parser = argparse.ArgumentParser(description='A Windows system tray utility to type special characters')

parser.add_argument('--customize_characters.json', '-c', help='print the path to characters.json')